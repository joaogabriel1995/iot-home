import { createTheme } from '@mui/material/styles'
export const DarkTheme = createTheme({
  palette: {
    primary: {
      main: '#fff'
    },
    background: {
      default: '#292738',
      paper: '#fff'
    }
  }
})
