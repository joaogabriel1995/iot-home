# Exemplo .env
# Criar um arquivo .env na raiz do backend.
# backend > .env

# Porta referente a API. Exemplo : 4000, 4005
API_PORT=

# Broker

# IP do broker que a aplicação conectará. Exemplo: 192.168.15.1 (IP do Broker)
HOST_BROKER=localhost

# Porta do broker que a aplicação conectará. Exemplo: 1883 
PORT_BROKER=

# User que está configurado no broker. Exemplo: admin (IP do Broker)
USER_BROKER=admin

# Senha broker que a aplicação conectará. Exemplo: admin123 
PWD_BROKER=admin123